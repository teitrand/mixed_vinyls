{% extends 'base.html.twig' %}
{% block title %}Create a new Record | {{ parent() }}{% endblock %}
{% block body %}
    <h1>{{ title }}</h1>

    {# TODO Add images #}

    <div>
        Tracks:
        <ul>
            {% for track in tracks %}
                <li>
                    {{ track.song }} - {{ track.artist }}
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
